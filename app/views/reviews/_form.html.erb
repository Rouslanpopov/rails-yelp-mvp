<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= simple_form_for [@restaurant, @review] do |f| %>
        <%= f.input :content %>
        <%= f.input :rating, collection: Review::RAT %>
        <%= f.submit class: 'btn btn-primary' %>
      <% end %>
    <%= link_to "Back", restaurant_path(@restaurant) %>

    </div>
  </div>
</div>
